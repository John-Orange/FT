<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Fitness Tracker"
        android:textSize="24sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"/>

    <Button
        android:id="@+id/btnTrackActivity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Track Activity"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/tvTitle"
        android:layout_marginTop="30dp"/>

    <Button
        android:id="@+id/btnBmiCalculator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BMI Calculator"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btnTrackActivity"
        android:layout_marginTop="20dp"/>

    <Button
        android:id="@+id/btnGoalActivity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Goal Activity"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/btnBmiCalculator"
        android:layout_marginTop="20dp"/>
</RelativeLayout>
